+ BUG: sometimes <hash> in link to gitweb not present
    Example: 
        ASE-2 
        ITEM-0106
        HASH = 996d84f
    but link is 
        http://172.16.20.64/swver_hist/gitweb/?p=bourevest/ase-2.git;a=shortlog;cm=fix%20board%20determination;h=
    must be something like:
        http://172.16.20.64/swver_hist/gitweb/?p=bourevest/ase-2.git;a=shortlog;cm=fix%20board%20determination;h=996d84f
    No more place?
+ intro page -> departments and devices only lists
+ rename version column "Хэш сумма" => "Версия"
+ rename column "Устройство" => "Прибор"
+ add to column "Версия" date and time of commit: <hash> <YYYY-MM-DD> <HH:MM>
+ JavaScript selection: sometimes several commits selected due same comments
+ change "БРА-135Ф" => "БРА-135F"
+ add setted up config files to srv-swdev repo (backup)
+ add link to ftp to redistributives in form:

    ftp://srv-swdev/redist/bourevestnik.ru/<device_class_en>/<commit_hash>

    - show only for "for all" versions
    - link must be in columns "Version" with title "[R]" example

        item 66 : .... : 491845 2017-04-22 16:33
        for all : .... : 491845 2017-04-22 16:33 [R]
    
- add "metric" column after version column. Metric for tags A and B for one device class defined as:
 
    def: INST(A, B) - install date and time diff:       -,0,+     A < B => "-",  A = B => "0"  A > B "+"    A relative to B
    def: DIST(A, B) - direct path commits count diff: x,-,0,+ 
    
    Values:
    
        -   : lesser
        0   : equals
        +   : greater
        *   : any
        x   : not exist
    
    def: DIST(A, B) = x   =>   A and B in different branches without direct way to each other
    
    ISB - [I]tem [S]pecific [B]ranch name is in form "release/<device_class>/item-<id>"
        
    if A in ISB              : x => DIST(A, B) = '+'
    if A in any other branch : x => DIST(A, B) = '-'

    DI(A, B)   B - is the BASE commit, A - testable, D - DIST, I - INST
    ===:==A is==:==============================================================:===============
    x* : ****** : see branch specialization                                    :
    -+ : FORCED : A forced to install old version                              :
    -* : OLD    : A < B                                                        :
    0* : OK     : A == B                                                       :
    +- : OK+EXP : Too scary to move base upper, but A is ok or EXPerimental    :
    +* : OK     : A >= B                                                       :
    xxx: ERROR  : base is not exists
    
    Realized
    
    def: CREAT(A, B) - commit creation date:     x,-,0,+
    
    CURRENT realized: 
    
    CI (A, B)   B - is the BASE commit, A - testable, I - INST, C - commit create 
    ===:==A is=====:==============================================================:=========:==============:=============:
    -+ : FORCED OLD: YLW :                                                        : FORCED  : FORCED OLD   : GRN+RED/BLK :
    -- : FORCED BB : R+G : why FORCED? m.b. simply old?                           : OLD     : OLD          : YELLOW      :
    0* : BASE      : GRN : Last by CMT.DATE - ? m.b. simply equals to base?       : OK      : BASE         : GRN+GRN     :
    +- : EXP_CAN   : BLO : Cancelled experimental?                                : OK+EXP  : EXPERIMENTAL : BLU+BLU     :
    ++ : EXP       : BLU :                                                        : OK      : BASE+        : GRN+BLU     :
    ?? : OLD       : RED :                                                        : ERROR   : ERROR        : RED         :
    
    Show in metric:
        <RES> (d/c)
        <RES> ::= 
            OK     |    A new            (green)
            OK/EXP |    A new and exp    (green)
            FORCED |    A old but forced (yellow)
            OLD         A old            (red)
            
    d - days between commits     (positive is A > B)
    c - commits between commits  (positive is A > B)

    for "item" or "order" lines -> A is line version, B - base version
    for "all versions" lines -> A is line version, B - latest installed version to specific device
    
- production versions domains. Tag names are:

    PROD/<device_class>/ITEM-<id>/<time_stamp>[/<domains>]
    PROD/<device_class>/ORDER-<id>/<time_stamp>[/<domains>]
    PROD/<device_class>/<time_stamp>[/<domains>]
    
    <device_class> ::= ASW-1 | ...
    <id> ::= 0000 | 0001 | ...
    <time_stamp> ::= YYYY-MM-DD-HHMM
    
    <domains> ::= <domain> | <domain>/<domains>
    <domain> ::= "linux" | "dos" | ...
    
    
    - show "for all" version for each found domains and for root domain
    - check versions metrics with base in same domain or in upper domain (if same domain base is not exists)
    
+ for each repo add sw name prefix in config:

    HL:asv-1_v2_hl.git
    DRV:asv-1_ll_drv.git
    
    and if
        asv-1_v2_hl.git  have tag PROD/ASW-1/ITEM-0014/2017-xx-xx-xxxx
        asv-1_ll_drv.git have tag PROD/ASW-1/ITEM-0014/2017-xx-xx-xxxx
    then show table in table
        | ASW-1 | #14 | HL | 2017-xx-xx-xxxx
                        DRV| 2017-xx-xx-xxxx
